#ifndef _REJFPANK_TCONTROLSTATE
#define _REJFPANK_TCONTROLSTATE

struct TControlState {
	GLsizei winWidth;
	GLsizei winHeight;

	//bool keyMap[KEYS_COUNT];
	//bool controlMap[CONTROLS_COUNT];

//	void switchControl(const int ctrl);
} controlState;

#endif // !_REJFPANK_TCONTROLSTATE
